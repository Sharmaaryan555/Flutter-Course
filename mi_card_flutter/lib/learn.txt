import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        backgroundColor: Colors.teal,
        body: SafeArea(
          child: /*Column*/Row(//It also work same as for row
            // mainAxisSize: MainAxisSize.min, // This Occupying the Minimum space in a column
            //verticalDirection: VerticalDirection.up, // It Changes the direction from where to start
            // crossAxisAlignment: CrossAxisAlignment.end,
            // mainAxisAlignment: MainAxisAlignment.spaceEvenly, // This will give the space Evenly
            crossAxisAlignment: CrossAxisAlignment.stretch,
            children: [
              Container(
                height: 100.0,
                width: 100.0,
                // margin: EdgeInsets.all(20.0),
                // padding: EdgeInsets.only(top: 10.0),
                color: Colors.white,
                child: Text('Container 1'),
              ),
              SizedBox(
                // height: 20.0,
                width: 20.0,
              ),
              Container(
                height: 100.0,
                color: Colors.blue,
                child: Text('Container 2'),
              ),
              Container(
                height: 100.0,
                color: Colors.red,
                child: Text('Container 3'),
              ),
              // This is an Invisible Container used for CrossAxisAlignment.end
              // Container(
              //   height: 10.0,
              //   width: double.infinity,
              //   color: Colors.pink,
              // )
            ],
          ),
        ),
      ),
    );
  }
}
